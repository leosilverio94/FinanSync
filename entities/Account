{
  "name": "Account",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Nome da conta"
    },
    "type": {
      "type": "string",
      "enum": [
        "checking",
        "savings",
        "wallet"
      ],
      "description": "Tipo da conta"
    },
    "bank": {
      "type": "string",
      "description": "Banco"
    },
    "balance": {
      "type": "number",
      "description": "Saldo atual"
    },
    "color": {
      "type": "string",
      "description": "Cor para exibi\u00e7\u00e3o"
    }
  },
  "required": [
    "name",
    "type",
    "balance"
  ],
  "rls": {
    "create": {
      "created_by": "{{user.email}}"
    },
    "read": {
      "created_by": "{{user.email}}"
    },
    "update": {
      "created_by": "{{user.email}}"
    },
    "delete": {
      "created_by": "{{user.email}}"
    },
    "write": {
      "created_by": "{{user.email}}"
    }
  }
}